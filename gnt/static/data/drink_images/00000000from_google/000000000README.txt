If get_images.py outputs a folder here, you have to delete all the images 
in the folder except for 1  and remove the __# from the end of that file name
then move the folder up one level to drink_images. 